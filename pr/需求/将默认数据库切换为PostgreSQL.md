# 将默认数据库切换为 PostgreSQL

- 需求编号：`需求-01`
- 首次提出：`2026-01-27`
- 最后有效确认：`2026-01-27`
- 时效状态：`有效`
- 实现状态：`已落地`
- 需求主线：`基础环境与默认配置`

## 当前定义

- 将当前二开仓库默认使用的 S2 数据库替换为用户给定的 PostgreSQL 实例，并验证核心接口可用。

## 用户要求

- 使用用户提供的 PostgreSQL 连接信息替换原默认数据库。
- 以新数据库作为当前默认运行环境，而不是只做一个可选示例配置。
- 修改后至少要通过实际接口访问验证连接可用。

## 需求边界

- 这条需求只处理“默认数据库切换”本身。
- 切库后暴露出的语义查询、指标定义或解析问题，不自动归入本需求。

## 验收要点

- 默认配置已经指向 PostgreSQL。
- 应用能够基于 PostgreSQL 正常启动并访问核心接口。
- 不再依赖旧的默认数据库配置才能完成基本运行。

## 归属的 bug / 修复

- 暂无需要长期保留的独立问题卡。

## 关系

- 当前没有明确的替代或依赖关系。

## 来源会话

- `2026-01-27` `019bfe3f-2a8a-7f70-9db2-3c980237a907` 用户提供 PostgreSQL 连接信息，要求替换当前 S2 数据库。
- `2026-01-27` `019bfe4c-1bcc-79a3-9a08-97a841526576` 继续围绕 PostgreSQL 默认配置和接口可用性推进。
