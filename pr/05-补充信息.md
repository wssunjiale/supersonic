# Supersonic 补充信息

## 人工复核后的归类提醒

- `2026-01-22` 的早期长会话同时支撑 `需求-03`、`需求-04`、`需求-05`、`需求-07`，不能只挂在 `需求-03` 下。
- `需求-13` 的范围不能只理解为“找 runner 入口”；它还包括失败重试、失败原因管理、错误 SQL 禁止同步和手动同步入口。
- `需求-15` 与 `需求-17` 要区分：
  - `需求-15` 处理的是 Schema Mapping、few-shot、字段选择与 SQL 改写策略。
  - `需求-17` 处理的是空指针、脏数据、反序列化、字段名解析等稳定性问题。
- `需求-21` 是独立新需求，不能被 `需求-08` 吞掉；`需求-08` 只保留“选图和参数资产”本身。
- `需求-11` 保留为“已过时”是合理的，因为它被 `需求-12` 在同一轮认知澄清中明确替代。

## 尚未升格为正式需求的线索

- `2026-01-27` `019bfec8`：历史异常日志排查，更像排障碎片，不足以单独形成需求。
- `2026-01-27` `019bff7b`：查 Superset 文档的资料检索会话，不是需求。
- `2026-02-03` `019c2333`：Superset Public 角色与权限兼容信息，暂时更适合作为 `需求-04` 的补充背景。
- `2026-02-05` `019c2b49`：历史异常日志，不足以抽成独立需求。
- `2026-02-14` `019c5cb7`：查 Swagger 地址的资料检索会话，不是需求。
- `2026-03-04` `019cb6fe`：中断会话，不进入正式需求。

## 仍值得保留的问题线索

- `2026-01-30` `019c0d8e`：Superset sync 日期格式 / 422 兼容问题，后续仍应挂到 `需求-13`。
- `2026-02-04` `019c2470`：重复创建 database / dataset 返回 `already exists`，属于 `需求-13`。
- `2026-02-26` `019c9a63`：`Request method 'GET' is not supported`，目前还不足以稳定归类。
- `2026-03-02` `019cacf3`：Superset sync 500 fatal error，属于 `需求-13` 的兼容与排障线索。

## 整理时明确排除的内容

- 当前清理会话中的讨论与判断，不直接当作历史需求来源。
- 脚本生成的 JSON、缓存和 `__pycache__` 不是需求知识库的一部分。
